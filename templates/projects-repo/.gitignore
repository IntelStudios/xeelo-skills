# GraphQL connection (parent xeelo-skills .gitignore does not apply here)
**/.xeelo-connection.json
**/.xeelo-connection.example.json

# DB transfer snapshots — re-download with /download-db
**/snapshots/**/*.zip
**/snapshots/**/*.xml
**/snapshots/**/*.json
