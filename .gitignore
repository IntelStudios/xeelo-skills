__pycache__/
*.pyc
.venv/
venv/
.venv/
.DS_Store

# Site working copies — never in the public KB
/projects/

# Local product-source index — never in the public KB
/internal/

# Per-project GraphQL connection (xeeloUrl + token) — never commit
**/.xeelo-connection.json
